---
layout: base
title: Design Notes
---

<section>
  <h1>Design Notes</h1>
  <ul>
    {% for post in collections.designNotes %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a>
        {% if post.data.description %}<p>{{ post.data.description }}</p>{% endif %}
      </li>
    {% endfor %}
  </ul>
</section>
